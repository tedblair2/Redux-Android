query Country($code:ID!) {
    country (code: $code) {
        code
        capital
        currency
        emoji
        languages {
            name
        }
        name
        continent {
            name
        }
    }
}